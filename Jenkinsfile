pipeline {
  agent any
  stages {
    stage ('build') {
         steps {
              echo 'hai all'
             }
           }
    stage ('build1') {
      steps {
         sh 'bash preethi.sh'
      }
    }
    stage ('test') {
      steps {
         sh 'bash nish.sh'
      }
    }
  }
}
