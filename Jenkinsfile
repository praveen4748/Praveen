pipeline {
  agent any
  stages {
    stage ('build') {
         steps {
              echo "hai all"
             }
           }
    stage ('build1') {
      steps {
         bash preethi
      }
    }
    stage ('test') {
      steps {
         bash nish
      }
    }
  }
}
