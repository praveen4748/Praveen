pipeline {
  agent any
  stages {
    stage ('build') {
         steps {
              echo 'hai all'
             }
           }
    stage ('build1') {
      steps {
         sh 'preethi'
      }
    }
    stage ('test') {
      steps {
         sh 'nish'
      }
    }
  }
}
